[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node -e \"console.log('ENV KEYS:', Object.keys(process.env).sort().join(', '))\" && npx prisma migrate deploy && node dist/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
