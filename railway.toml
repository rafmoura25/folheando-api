[build]
builder = "NIXPACKS"
buildCommand = "npm install --ignore-scripts && DATABASE_URL=postgresql://placeholder:placeholder@localhost:5432/placeholder npx prisma generate && npm run build"

[deploy]
startCommand = "npx prisma migrate deploy && node dist/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
